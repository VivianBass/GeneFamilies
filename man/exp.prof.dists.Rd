% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_funks.R
\name{exp.prof.dists}
\alias{exp.prof.dists}
\title{Compute Euclidean Distances Between Pairs of Gene Expression Values}
\usage{
\method{exp}{prof.dists}(
  gene.accessions,
  expression.profiles = rna.seq.exp.profils,
  expr.prof.gene.col = "id",
  tissues = setdiff(colnames(expression.profiles), c(expr.prof.gene.col)),
  dist.method = "euclidean",
  per.tissue = FALSE
)
}
\arguments{
\item{gene.accessions}{A vector of gene accession identifiers for which distances are
to be computed.}

\item{expression.profiles}{A data frame or tibble containing gene expression profiles
with gene IDs in one column and expression values in other columns. Default is
\code{rna.seq.exp.profils}.}

\item{expr.prof.gene.col}{A string specifying the column name in \code{expression.profiles}
that contains the gene IDs. Default is \code{"id"}.}

\item{tissues}{A character vector of column names representing tissues or conditions
to be used for calculating distances. Default is all columns in \code{expression.profiles}
except \code{expr.prof.gene.col}.}

\item{dist.method}{A string specifying the distance calculation method. Default is
\code{"euclidean"}.}

\item{per.tissue}{A logical value. If \code{TRUE}, computes distances separately for each
tissue; if \code{FALSE}, computes distances across all tissues together. Default is \code{FALSE}.}
}
\value{
A distance matrix if \code{per.tissue = FALSE}, or a list of distance objects
(one per tissue) if \code{per.tissue = TRUE}. Returns \code{NA} if there are fewer than
two expression profiles.
}
\description{
Calculates pairwise Euclidean distances between gene expression profiles
for a given set of genes. Optionally, distances can be computed separately for each
tissue.
}
\examples{
# Compute Euclidean distances for a set of genes across all tissues
distances <- exp.prof.dists(gene.accessions = c("gene1", "gene2", "gene3"))

# Compute distances per tissue
tissue_distances <- exp.prof.dists(gene.accessions = c("gene1", "gene2", "gene3"), per.tissue = TRUE)
}
