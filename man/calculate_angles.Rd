% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/angles_funks.R
\name{calculate_angles}
\alias{calculate_angles}
\title{Calculate Angles for a Gene Group}
\usage{
calculate_angles(genes, rna.seq.exp.profils, tissues)
}
\arguments{
\item{genes}{A list of gene groups where each element is a vector of gene identifiers.}

\item{rna.seq.exp.profils}{A data frame containing RNA-seq expression profiles.
Must include a column \code{FBpp_ID} for gene identifiers and additional columns for tissue-specific expression levels.}

\item{tissues}{A vector of column names representing tissues in \code{rna.seq.exp.profils} for angle calculation.}
}
\value{
A data frame containing the following columns:
\describe{
\item{FBpp_ID}{Gene identifiers matching the input \code{genes}.}
\item{angle.diag}{Calculated diagnostic angle values for each gene.}
}
If no matching genes are found, a warning is issued and an empty data frame is returned.
}
\description{
This function calculates diagnostic angles for a given set of genes based on RNA-seq expression profiles.
}
\details{
The function computes angles using the \code{cosDiag} function applied to tissue expression levels for each gene, normalized by \eqn{\sqrt{2}}.
Only genes present in both the \code{genes} list and the \code{FBpp_ID} column of \code{rna.seq.exp.profils} are processed.
NA or invalid values are filtered out from the resulting data frame.
}
\note{
Ensure that the \code{cosDiag} function is defined in your environment and accepts the appropriate input format.
}
\examples{
\dontrun{
# Example data
genes <- list(group1 = c("gene1", "gene2"), group2 = c("gene3", "gene4"))
rna.seq.exp.profils <- data.frame(
  FBpp_ID = c("gene1", "gene2", "gene3"),
  tissue1 = c(1.2, 0.8, 1.1),
  tissue2 = c(0.9, 1.0, 0.7)
)
tissues <- c("tissue1", "tissue2")

# Compute angles
calculate_angles(genes, rna.seq.exp.profils, tissues)
}

}
