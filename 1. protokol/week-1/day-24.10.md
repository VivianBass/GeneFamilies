

**Date**: 24.10.2024  
**Git Branch**: `Gene-Families-tests-Andre`

---

**Tasks**:
1. **Documentation Updates**:
- Updated section and input file descriptions; added R package setup instructions. 

2. **Script Review**:
- Identified duplicate statistical R scripts. need to choose between versions from previous and new packages.

3. **Plot Generation**:
- Created plots in `results` folder using `plot_exp.prof.dists_distributions.R` with alternative data until `all.cds` and `all-vs-all` file needs are clarified.
- Improved plot aesthetics.

**Doubts & Issues**:
- Confirm if both `all-vs-all` and `pairwise similarities` data are necessary.
- Clarify the use of `all.cds` and whether to use BLAST or DIAMOND for `all-vs-all`.
- Standardize data naming and generalize loading scripts.

**Next Steps**:
- Separate T-tests from plot scripts if needed.
- Finalize input/output naming conventions for clarity.
- Standardize data naming and generalize loading scripts.
