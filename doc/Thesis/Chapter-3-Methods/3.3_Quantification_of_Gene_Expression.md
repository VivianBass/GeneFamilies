## **Quantification of Gene Expression**

### **Workflow Implementation**

   - _Lab-book:_ 

      - Sequential processing through Slurm scripts
      - Automated pipeline execution on bioserver
      - Standardized output formats for downstream analysis


- **Purpose**: Processing raw RNA-seq data into quantified gene expression values

   - Ensures accurate gene mapping and expression quantification across species
   - Enables reliable cross-species expression comparisons


### **Analysis Tools**


1. **GFFread**

   - *Purpose*: Reference Transcriptome Creation with GFFread

      - Creates a comprehensive collection of transcript sequences (mRNA, RNA)
      - Essential for accurate RNA-seq quantification and differential expression analysis
      - Combines genomic sequence with annotation informationCreates reference transcriptomes

   - *Required Input Files*: 

      1. **Genomic Sequence** (FASTA format)
         - Contains complete genome nucleotide sequences
         - Example: `/path/to/reference.fsa`

      2. **Genomic Annotations** (GFF format)
         - Defines gene structures and features
         - Includes exons, introns, CDS locations
         - Example: `/path/to/reference.gff`

   - *Output*: 

      - Transcriptome FASTA file - FASTA file containing transcript sequences

   - *Function*: 

      - Combines genomic annotations with sequences for RNA-seq alignment

   - *Command*: 

```bash
         gffread /path/to/reference.gff \
         -g /path/to/reference.fsa \
         -w /path/to/output_transcriptome.fa

         # - `-g`: Input genome FASTA file
         # - `-w`: Output transcriptome FASTA file
```   

# -------------------------------------------------------------------------------------

2. **Trimmomatic**

   - *Purpose*: Quality control of raw reads

         - Tool for cleaning and filtering raw RNA-seq data
         - Processes paired-end FASTQ files
         - Improves data quality for downstream analysis


   - *Input*: Raw FASTQ files

      - **Raw FASTQ Files**

         - Forward reads (`*_1.fastq`)
         - Reverse reads (`*_2.fastq`)
         - Contains sequences and quality scores

   - *Output*: Filtered FASTQ files
   - *Function*: 

        - Removes adapter sequences
        - Trims low-quality bases
        - Filters short reads (<70 bp)
        - Ensures high-quality input for quantification

   - *Command*: 

```bash
         java -jar trimmomatic-0.39.jar PE -threads 4 \
         input_1.fastq input_2.fastq \
         output_1T.fq.gz output_1U.fq.gz \
         output_2T.fq.gz output_2U.fq.gz \
         ILLUMINACLIP:TruSeq3-PE.fa:2:30:10:2:keepBothReads \
         MINLEN:70

         #- **PE**: Paired-end mode
         #- **-threads**: Number of processing threads
         #- **ILLUMINACLIP**: Adapter removal settings
         #- **MINLEN**: Minimum read length threshold
```   

# -------------------------------------------------------------------------------------

3. **Kallisto**

   - *Purpose*: Gene-Expression quantification

         - Fast, alignment-free RNA-seq quantification tool
         - Uses pseudoalignment for transcript abundance estimation
         - Provides accurate expression measurements in TPM (Transcripts Per Million)

   - *Input*: Filtered reads + Reference transcriptome
   - *Output*: Gene expression values (TPM)

         - Transcript-level abundances (TPM)
         - Bootstrap estimates for uncertainty
         - Organized by experimental condition

   - *Function*: 

        - Performs pseudoalignment of reads
        - Quantifies transcript abundance
        - Generates normalized expression values

   - *Command*: 

### **1. Index Creation**

```bash
# Build Kallisto index from reference transcriptome
kallisto index \
  -i results/transcriptome.idx \
  results/gffread_output.fa
```
- **Input**: Reference transcriptome (FASTA)
- **Output**: Indexed transcriptome file (.idx)
- **Purpose**: Enables rapid pseudoalignment


### **2. Expression Quantification**

```bash
# Quantify expression for paired-end reads
kallisto quant \
  -i results/transcriptome.idx \
  -o results/condition_name \
  -b 100 \
  reads_1T.fq.gz \
  reads_2T.fq.gz

   #- `-i`: Path to index file
   #- `-o`: Output directory
   #- `-b`: Bootstrap samples (default: 100)
   #- Input files: Paired-end trimmed FASTQ files
```    

# -------------------------------------------------------------------------------------


- *TPM Value Extraction*

- Kallisto provides TPM (Transcripts Per Million) values directly in the abundance.tsv output file. These values represent transcript-level quantification. For gene-level analysis, TPM values must be aggregated across all transcripts belonging to the same gene. This step focuses on extracting and processing TPM values from Kallisto output to calculate overall gene expression levels.

- Post-Processing Analysis
After Kallisto quantification, we processed the results using a Python script to analyze four different conditions:

   Whole body diet M
   Whole body diet P
   Muscle diet M
   Muscle diet P

The script performs the following operations:

Mapping File Processing: Links each transcript to its corresponding gene using a transcript-to-gene mapping file
Kallisto Output Processing: Parses the abundance.tsv file containing transcript-level TPM values
Value Aggregation: Calculates gene-level TPM by summing values from all transcripts of the same gene
Results Generation: Creates gene_level_tpm.txt with aggregated values, including means across three replicates for each condition












Also I checked your methods part and you have to modify the commands for trimmomatic and kallisto because those are pair end data and in the data that we downloaded we have single end 

so you have to modify the commands

I will also send you the stadistics for trimmomatic

like how many reads where filtered, etc. You have to think a way to show them

in material you also have to say the number of families that you are working with, why are you working only with those, etc